package templates;

import migrator.Input;
import mjson.Json;

public class Genre {

	static String FILE_PATH = "/movie_files/genres";
	static String ID_KEY    = "id";
	static String NAME_KEY  = "name";
	
	public static Input template() {
		
		return new Input(FILE_PATH) {
			
			@Override
			public String template(Json data) {
				
				return "insert $genre isa Genre"
						+ " has genre-id " + actor.at(ID_KEY).asInteger() + 
						  " has genre-name \"" + actor.at(NAME_KEY).asString() + "\";";
			}
		};
	} 
}
