package templates;

import migrator.Input;
import mjson.Json;

public class Book {

	static String FILE_PATH = "/book_files/authors";
	static String ID_KEY    = "id";
	static String NAME_KEY  = "name";
	
	public static Input template() {
			
		return new Input(FILE_PATH) {
			
			@Override
			public String template(Json book) {
				
				return "insert $book isa Book"
						+ " has author-id " + book.at(ID_KEY).asInteger()
						+ " has author-name \"" + book.at(NAME_KEY).asString() + "\";";
			}
		};
		
	}
}
